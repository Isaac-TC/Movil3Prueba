import 'package:flutter/material.dart';

class pantalla3 extends StatelessWidget {
  const pantalla3({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar:AppBar.new(),
      
      body: Center(child: Text("SCREEN 03"),),
    );
  }
}